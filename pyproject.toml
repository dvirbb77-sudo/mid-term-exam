[tool.poetry]
name = "mid-term-exam"
version = "0.1.0"
description = ""
authors = ["Bibi <dvirbb77-sudo>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.0.0"
gunicorn = "^21.2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
